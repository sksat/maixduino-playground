[project]
name = "maixduino-test-tools"
version = "0.1.0"
description = "Flashing / serial tooling for the Maixduino (K210) project, pinned via uv"
requires-python = ">=3.9"
dependencies = [
    # K210 UART ISP flashing utility. Used as `uv run kflash ...`.
    "kflash>=1.1.6,<2",
]
