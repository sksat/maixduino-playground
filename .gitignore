# Rust
/target

# build artifacts
*.bin

# uv / Python
.venv/
__pycache__/

# Camera captures (local only, not committed)
/captures/

# WiFi credentials (local only, never committed)
/wifi_creds.env

# ESP32 raw flash dump (the .gz is committed instead)
/esp32-firmware/*.bin

# PlatformIO build output
/esp32-modem/.pio/
